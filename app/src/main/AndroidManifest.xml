<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.junburg.rockbottom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity" android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.InputInfoActivity" android:theme="@style/AppTheme2"></activity>
        <activity android:name=".main.MainActivity" android:theme="@style/AppTheme2"></activity>
        <activity android:name=".myinfo.EditInfoActivity" android:theme="@style/AppTheme2"></activity>
        <activity android:name=".learn.LearnActivity" android:theme="@style/AppTheme2"></activity>
    </application>

</manifest>